# Static icons for /share resource cards

Place SVG or PNG files here, then set the resource **icon** field to:

`/icons/your-file.svg`

Example: `public/icons/bilibili.svg` → icon value `/icons/bilibili.svg`

These files are served from the site root and do not use Redis storage.